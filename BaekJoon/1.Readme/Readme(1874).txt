// 스택 수열 (1874) Silver[2] 설계



#1  // 실패 
1) input()
2) solve()
	for() 하나씩 올라가면서 
		넣고 다음 것도 같으면 넣고 push
			다음 것 다르면 push & pop
			target이 다음 for문에서 다음 나올 숫자가 아니라 지나간 숫자면 나올때까지 pop
3) stack 에 온전히 저장되어 있으면 명령어 출력
	아니면 NO 출력



#2  // (성공) 입력 받으면서 해보기 
1) input() 
	// n
2) solve()
	cin 여기서 입력 받으면서 받은 거 바로 if 문으로 
	push or pop 하면서 최종 배열에 담고
3) 결과가 담겨있는 배열 출력 !

※ result(마지막 결과 담는 배열)을 char 배열[] 로 만드니까 Out Of Bounds 가 나오잖아 얼마나 받을지 모를 땐
	vector 쓰면 돼 괜히 배열로 만드니까 idx 변수도 필요하고 사이즈 얼마나 해야되는지도 모르고 그러지마 ... 