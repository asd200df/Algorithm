// 가장 긴 증가하는 부분 수열 2 (12015) Gold[2]




#1 
0) Lower_bound 이용하기 
1) input && solve()
	// vector 사용
	// 값을 받으면서 vec back() 에 있는 값 보다 크면 push back
	// 작으면 lower_bound 사용해서 값 교체
	// 모든 입력이 끝나면
2) print vector.size() !