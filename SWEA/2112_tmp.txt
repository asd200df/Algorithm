// 2112. 보호 필름
#include <iostream>
#include <string>
#include <cstring>
#include <stack>
#include <queue>
#include <vector>
#include <cmath>
#include <algorithm>
#include <unordered_map>
#include <time.h>

using namespace std;

// Global
int D, W, K, map[15][22], backup[15][22];

bool isValid() {  // Valid Check func
	for (int x = 0; x < W; x++) {
		for (int y = 0; y < D; y++) {
			if(map[])
		}
	}
	return true;
}

int solve() {  // solution func
	if (isValid()) { return 0; }
	else {

	}
}

void input() {  // input func
	cin >> D >> W >> K;
	for (int i = 0; i < D; i++) {
		for (int j = 0; j < W; j++) {
			cin >> map[i][j];
			backup[i][j] = map[i][j];  // map >> backup
		}
	}
}

void init() {  // initialization func
	// pass
}

int main() {  // MAIN
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int T;
	cin >> T;

	for (int tc = 0; tc < T; tc++) {
		init();  // initialization func call
		input();  // input func call

		int result = solve();  // solution func call & input
		cout << "#" << tc + 1 << " " << result << "\n";  // print result
	}

	return 0;
}