// Softeer 택배 마스터 광우 문제 설계

# 1
1) input 받기
2) 레일 순서를 DFS로 랜덤 저장 
3) DFS에 종료 조건에 순서가 다 정해지면 valid 함수로 넘어가
4) valid 함수에서 무개를 재 >> 그 다음 최솟값 비교
5) 갱신한 최솟값 출력 !
used 사용 ? ㅇㅅㅇ 

# 2
1) input 받기
2) 레일 순서를 DFS로 랜덤 저장 
3) DFS에 종료 조건에 순서가 다 정해지면 sum 함수로 넘어가
4) sum 함수에서 무개 변수 하나에 저장하고
5) 그 변수랑 최솟값 비교후 갱신
6) 갱신한 최솟값 출력 !
